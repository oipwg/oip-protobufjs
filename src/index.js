import { json, staticModules } from './compiled'
import {
  buildOipFiveTemplate,
  buildRecordTemplate,
  buildSignedMessage,
  signMessage,
  templateBuilder,
  buildDescriptor,
  decodeDescriptor,
  recordProtoBuilder,
  buildOipDetails
} from './builders'

export {
  json as ProtoModulesJson,
  staticModules as ProtoModules,
  buildOipFiveTemplate,
  buildRecordTemplate,
  buildSignedMessage,
  signMessage,
  templateBuilder,
  buildDescriptor,
  decodeDescriptor,
  recordProtoBuilder,
  buildOipDetails
}
